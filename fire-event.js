// const io = require('socket.io-client');
// const host = 'http://localhost:3001';
// const fireConnect = io.connect(`${host}/caps`);


// fireConnect.emit('pickup',order);
// fireConnect.emit('in-transit',order)
// fireConnect.emit('delivered',order)


